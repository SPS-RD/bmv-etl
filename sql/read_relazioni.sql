SELECT IDRELAZIONE     AS relazione_id,
       IDPROMOZIONE    AS promozione_id,
       DESCRIZIONE     AS descrizione,
       TIPORELAZIONE   as tipo_relazione,
       NUMMINIMOEVENTI as num_minimo_eventi
FROM PV_RELAZIONE